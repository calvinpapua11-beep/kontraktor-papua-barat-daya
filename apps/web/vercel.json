{
    "version": 2,
    "buildCommand": "npm run build",
    "framework": "react-router",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index"
        },
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}